{
  "name": "grpc-ui-mock-server",
  "version": "0.1.0",
  "private": true,
  "description": "Local reflected gRPC mock server for gRPC UI development",
  "main": "dist/server.js",
  "scripts": {
    "dev": "ts-node --project tsconfig.json src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.14.3",
    "@grpc/proto-loader": "^0.8.0",
    "@grpc/reflection": "^1.0.4"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.2"
  }
}
